<h1>Cards#index</h1>
<p>※現在登録されているデータの一覧です。</p>
<table>
    <tr>
        <th>ID</th>
        <th>題名</th>
        <th>著者</th>
        <th colspan="2"></th>
    </tr>
    <% @cards.each do |card| %>
        <tr>
            <td><%= card.id %></td>
            <td><a href="/cards/show/<%= card.id %>"><%= card.title %></a></td>
            <td><%= card.author %></td>
            <td><a href="/cards/edit/<%= card.id %>">編集</a></td>
            <td><a href="javascript:delData(<%= card.id %>,'<%= card.title %>')">削除</a></td>
        </tr>
    <% end %>
</table>
<p><a href="/cards/add">新しいデータを入力</a></p>
<script>
    function delData(id,title){
        if (confirm('「' + title + '」のデータを削除しますか？')){
            document.location = "/cards/delete/" + id;
        }
    }
</script>